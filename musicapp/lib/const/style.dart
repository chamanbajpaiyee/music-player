import 'package:flutter/material.dart';

import 'colors.dart';

const champ = "champ";
ourStyle({color = whiteColor, double? size = 14}) {
  return TextStyle(fontFamily: 'champ', color: color, fontSize: size);
}
